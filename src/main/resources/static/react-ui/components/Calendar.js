import React from "react";
import "../styles/calendar.css";

const Calendar = () => {
    return (
        <div className="calendar">
            <h3>Calendar</h3>
            <textarea placeholder="[Calendar Placeholder]" />
            <button>Book →</button>
        </div>
    );
};

export default Calendar;

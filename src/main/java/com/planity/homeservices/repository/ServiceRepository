// src/main/java/com/planity/homeservices/repository/ServiceRepository.java
package com.planity.homeservices.repository;

import com.planity.homeservices.model.Service;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ServiceRepository extends JpaRepository<Service, Long> {
    // Méthodes custom possibles :
    List<Service> findByTitleContainingIgnoreCase(String keyword);
}